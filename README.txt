The functionalities should not work as there needs to be implemented a database! Please watch the video for app presentation.

In case there are any CORS related erorrs, please install a CORS extension in your browser, more exactly: "Allow CORS: Access-Control-Allow-Origin" and enable it. After that 
the code should work properly.

For the contact form in order to work, credentials of the "admin" need to be supplied. It is no need to use this data as we have a video demo of how the website works, 
including the form and the CanIRunIt functionality.